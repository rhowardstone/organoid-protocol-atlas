# Archives / scratch
*.zip

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Eval/run artifacts that are regenerated (keep organoid_demo/outputs as reference)
workspace/evals/results/
workspace/evals/transcripts/

# OS
.DS_Store

# Browser QA scratch / screenshots (may embed methods text)
.playwright-mcp/
*.png
landing*.png
recipe*.png

# Local Claude project dir
.claude/

# Generated public DB (built at Docker-image build time by serve/build_public_db.py;
# never commit the .db — exports/public/*.jsonl are the committed source of truth).
data/public/
*.db
