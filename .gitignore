# Archives / scratch
*.zip

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Eval/run artifacts that are regenerated (keep organoid_demo/outputs as reference)
workspace/evals/results/
workspace/evals/transcripts/

# OS
.DS_Store

# Browser QA scratch / screenshots (may embed methods text)
.playwright-mcp/
*.png
landing*.png
recipe*.png

# Local Claude project dir
.claude/

# Local-only, NON-redistributable data (also covered by nested .gitignores; explicit
# here as belt-and-suspenders so a scaled ingestion batch can never commit full text,
# figure images, local model predictions, or generated DBs). Per codex #18 review.
data/evidence_bundles/local/
data/predictions/local/
data/figures/local/
data/public/
*.db

# Regenerable semantic-retrieval vectors (R3); rebuilt by semantic_index.py build
data/index/
